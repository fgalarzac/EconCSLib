\documentclass[tikz,border=10pt]{standalone}
\input{../../../docs/tikz/dag_preamble.tex}

\begin{document}
\begin{tikzpicture}[
  x=1cm,
  y=1cm,
  every node/.append style={outer sep=4pt}
]

\dagPaperMetadata{Designing Informative Rating Systems}{Garg, Johari}{Manufacturing \& Service Operations Management 23(3)}{2020}{https://arxiv.org/pdf/1810.13028}

\dagPaperLegendRightOfMetadata{
\node[dag_model, dag_template_legend] (legDef) at (0,0) {source\\definition};
\node[dag_lemma, dag_template_legend] (legLemma) at (3.4,0) {formalized\\appendix result};
\node[dag_result, dag_template_legend] (legResult) at (7.2,0) {formalized\\main result};
}
\daglegend{(legDef)(legLemma)(legResult)}{Legend}

\begin{dagPaperBody}

\node[dag_model] (CountScore) at (0,0) {
  \textbf{Sample count and aggregate score} \\
  $n_k(\theta)=\lfloor k g(\theta)\rfloor$; average of exactly $n_k$ scores \\
  \emph{corrects the printed off-by-one index}
};

\node[dag_model] (Objectives) at (6.8,0) {
  \textbf{Pairwise and ranking objectives} \\
  strict correct-order minus inversion probability; \\
  uniform average $W_k$ over quality-ordered pairs
};

\node[dag_model] (RateDefs) at (13.6,0) {
  \textbf{Large-deviation definitions} \\
  log-MGF $\Lambda(z\mid\theta)$ and \\
  rate function $I(a\mid\theta)$
};

\node[dag_model, text width=5.1cm] (Conditions) at (0,-4.5) {
  \textbf{Source model conditions} \\
  positive match rates; monotone scores; ordinal \\
  tail dominance and strict tail decrease; strict span
};

\node[dag_lemma, text width=4.8cm] (ProbLess) at (7.0,-4.5) {
  \textbf{Appendix: \texttt{problessthan}} \\
  pairwise score-gap error has the displayed \\
  support-safe threshold rate
};

\node[dag_lemma, text width=4.8cm] (PkLD) at (13.8,-8.8) {
  \textbf{Appendix: \texttt{Pk\_LD}} \\
  the rate transfers to $1-P_k$
};

\node[dag_result, text width=5.8cm, minimum height=2.1cm] (TheoremOne) at (5.5,-13.0) {
  \textbf{Theorem 1} \\
  $1-W_k$ has the minimum over valid adjacent-pair rates, \\
  with the finite-support endpoint stated support-safely
};

\draw[dag_arrow] (CountScore) -- (Objectives);
\draw[dag_arrow] (RateDefs) -- (ProbLess);
\draw[dag_arrow] (CountScore) -- (ProbLess);
\draw[dag_arrow] (Conditions) -- (ProbLess);
\draw[dag_arrow] (Objectives) -- (PkLD);
\draw[dag_arrow] (ProbLess) -- (PkLD);
\draw[dag_arrow] (Conditions.south) to[out=-90,in=170] (TheoremOne.west);
\draw[dag_arrow] (PkLD.south west) to[out=-130,in=15] (TheoremOne.east);

\end{dagPaperBody}
\end{tikzpicture}
\end{document}
