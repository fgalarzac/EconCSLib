# Source Clarifications

This note records local source readings used for the checked mathematical
results. Each preserves the advertised endpoint.

| Source location | Clarified reading | Effect |
| --- | --- | --- |
| Theorem 1 no-zero clause | Capacity is positive and the applicant universe contains an applicant outside the chosen set. | Makes the stated nonzero conclusion nonvacuous. |
| Monotonicity/q-acceptance argument | One additional applicant is sufficient; the displayed route's request for two disjoint q-sets is unnecessary. | Preserves the monotonicity conclusion. |
| Removable-set lemma | The intended equality compares the two pools, rather than a set with itself. | Restores the displayed comparison. |
| Tightly-even corollary | The displayed `$dk$` is read as the positive even instability parameter `$d=2k$`. | Restores the stated no-positive-even-tightness conclusion. |
| Appendix indexing and membership displays | Local repeated indices, membership symbols, parentheses, and a self-cardinality display are read in their surrounding definitions. | Typographical only. |
| Strict-order completeness | Comparisons range over distinct pairs. | Matches the definition of a strict total order. |
| Theorem 2 queue characterization | The stated range and the `$m=1$` case are read for positive total capacity on a nontrivial applicant universe, with a one-order characterization rather than redundant queue copies. | Preserves the range and single-order characterization. |
| Exact q-representative variability | The representation is read minimally, so redundant copies do not manufacture additional variability. | Preserves the exact variability examples. |
| Linear-assignment application | Weights are real and tie-free, with the assignment optimizer satisfying the stated feasibility and optimality conditions. | Gives the application its intended order structure. |

These are source clarifications and local typos, not additional paper-level
caveats.
